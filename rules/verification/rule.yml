id: verification
class: verification
severity: medium
description: Verifies a downloaded artifact with a checksum or signature
kind: simple_command
match_values: [sha256sum, shasum, sha512sum, sha1sum, md5sum, cksum, gpg, gpgv, gpg2, gpgv2, cosign, minisign]
